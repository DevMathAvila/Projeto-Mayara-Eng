# Google Analytics 4.
# O Measurement ID já está definido como padrão no código (G-PNYX2Y268K) e é
# carregado apenas em produção. Defina a variável abaixo apenas se quiser
# sobrescrever o ID (por exemplo, usar outra propriedade em outro ambiente).
NEXT_PUBLIC_GA_ID=
